<div class="game-container" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path @playlist.picture %>)">
  <div id="game-container" data-game-id="<%= @game.id %>">

  <% if @game.paused? %>
    <%= render 'game_paused' %>
  <% end %>
<!-- game created -->
  <div id="test">

  </div>

  <% if @game.created? %>
    <div class="show-created-html" id="game-created-html">
      <!-- debut de la section a mettre dans partial -->
      <%= render 'created' %>
      <!-- debut de la section a mettre dans partial -->
    </div>
  <% end %>

<!-- ----------------------------------------- -->

<!-- game running  -->

  <% if @game.running? %>
  <%= render 'game_running',
    answering_time: @answering_time,
    current_track: @current_track,
    game: @game %>
    <!-- partie en cours -->
      <!-- ========================================== -->

    <% if @current_track %>
      <div id="running-html">

        <!-- message pour bonne ou mauvaise réponse -->
        <% if Answer.last %>
          <div class="speech-bubble speech-bubble-left">
            <p>
            <% if Answer.last.status == true %>
              <%= "Bien joué petit génie de la zik 🕺" %>
              <% elsif Answer.last.status == false %>
                <%= "Même ma petite soeur connaît ça... Try again ! " %>
              <% else %>
                <%= "nil" %>
            <% end %>
            </p>
          </div>
        <% end %>
        <!-- fin de la section mise dans partial par Victor -->


      </div>
    <!-- - - - - - - - - - - - -->
    <% else %>
    <!-- fin de partie - resultats -->
    <!-- résultats par chansons -->
      <div id="game-finished">
      <!-- debut de la section a mettre dans partial-finished -->
        <%= render 'game_running',
    answering_time: @answering_time,
    current_track: @current_track,
    game: @game %>
      <!-- fin de la section a mettre dans partial game-finished -->
      </div>
    <!-- ----------- -->
    <% end %> <!-- end du if @current_track -->
  <% end %> <!-- end du if game.running? -->

  <!-- game paused - formulaire de réponse -->
  <% if@game.paused? %>
    <div class='display-container'>

    </div>
  <% end %>
  <!-- -----------------------  -->
</div> <!-- div fermante du container -->


<!-- le code qui suit était déja commenté, a supprimer?? -->


<!-- <div id="message-modal">
  <div class="form-modal">
    <p> <strong><%= @user.pseudo %></strong> a été plus rapide que toi 🚀 il donne une réponse... </p>
  </div>
</div>
-->

