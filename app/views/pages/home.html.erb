<div id="homepage">
 <!--  <p class="nav-item text-right w-100">
    <%#= link_to "Login", new_user_session_path, class: "nav-link" %>
  </p> -->
 <div class="nav-item dropdown text-right w-100">
  <i class="fas fa-bars dropdown-toggle mr-3 mt-3 text-white" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" ></i>
  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
    <% if user_signed_in? %>
      <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      <%= link_to "Signup ", new_user_registration_path, class: "nav-link" %>
    <% end %>
  </div>
 </div>

<% RSpotify.authenticate(ENV["SPOTIFY_CLIENT_ID"], ENV["SPOTIFY_CLIENT_SECRET"]) %>
<% result = RSpotify::Track.search('another brick in the wall') %>
<div class="album-photo" style="background-image: url(<%= result[0].album.images[0]["url"] %>)" >
</div>

  <div class="content">
    <div class="image">
      <%= image_tag "image.png", class: "logo-gt" %>
    </div>
    <h1>GUESS WHAT</h1>
    <p>SHARE MUSIC ANYWHERE</p>
  </div>

  <% if user_signed_in? %>
    <div class="text-center">

        <%= simple_form_for @game do |f| %>
          <%= f.submit "CREE UNE PARTIE", class: "btn-home" %>
        <% end %>
    <%= link_to "REJOINS UNE PARTIE", new_participation_path , class: "btn-home2" %></p>

    </div>
  <% end %>
</div>




