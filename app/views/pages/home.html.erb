<div id="homepage">
<p class="nav-item">
  <%= link_to "Login", new_user_session_path, class: "nav-link" %>
</p>
    <div class="content">
      <div class="image">
        <%= image_tag "logo-guesswhat.png" %>
      </div>
      <h1>GUESS WHAT</h1>
      <h2>SHARE MUSIC ANYWHERE</h2>
    </div>

  <% if user_signed_in? %>
    <div class="text-center">
        <%= simple_form_for @game do |f| %>
          <%= f.submit "Créer une partie", class: "btn-home" %>
        <% end %>
    <%= link_to("Rejoindre party", new_participation_path , class: "btn btn-home") %></p>
    </div>
  <% end %>

</div>
